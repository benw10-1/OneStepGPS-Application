{
    "name": "device-tracker",
    "version": "1.0.0",
    "description": "OneStepGPS Application",
    "scripts": {
        "install": "cd client && npm install",
        "build": "cd client && npm run build",
        "start": "cd server && go run server.go"
    }
}
